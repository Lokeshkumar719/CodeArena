const LANGUAGE_IDS = {
  cpp: 54,
  java: 62,
  javascript: 63,
};

const MAX_POLLING_RETRIES = 10;

const POLLING_INTERVAL = 1000;

module.exports = {
  LANGUAGE_IDS,
  MAX_POLLING_RETRIES,
  POLLING_INTERVAL,
};
